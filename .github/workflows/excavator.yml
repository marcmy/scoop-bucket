on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - 'bucket/*.json'
      - 'scripts/**/*.reg'
  schedule:
    # run every 30 minutes
    - cron: '2,13,26,34,47,52 * * * *'
name: Excavator
jobs:
  excavate:
    name: Excavate
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@main
      - name: Excavate
        uses: ScoopInstaller/GithubActions@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SKIP_UPDATED: 1
